interface:
  display_name: "Tent Task"
  short_description: "Execute and submit one TaskResult inside a Tent"
  default_prompt: "Use $tent-task to execute this Tent Task. Also apply $tent-role when acting as a durable Role."
